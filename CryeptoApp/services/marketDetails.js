// import {MARKET_DETAILS} from '../assets/data/MarketDetails'

export const getMarketDetails =  () => {

    try{
        // const responce = await axios.get('https://api.coingecko.com/api/v3/global');
        // const data =  responce.data; 
        
        
        // const data =  MARKET_DETAILS.data.market_cap_change_percentage_24h_usd.toFixed(2);

        // const marketCapital = Object.values(MARKET_DETAILS.data.total_market_cap)
        // const marketVolume = Object.values(MARKET_DETAILS.data.total_volume)

        // var totalMarketCapital = 0;
        // var totalMarketVolume = 0;
       
        // for ( var i = 0; i <= 50; i ++) totalMarketCapital += marketCapital[i];        
        // for ( var i = 0; i <= 50; i ++) totalMarketVolume += marketVolume[i];
        
        // const newData = {
        //     top50MarketCapital : totalMarketCapital.toFixed(2),
        //     top50MarketVolume : totalMarketVolume.toFixed(2),
        //     marketCapitalChangePercentage24h : MARKET_DETAILS.data.market_cap_change_percentage_24h_usd.toFixed(2),
        //     activeCryptoCoins : MARKET_DETAILS.data.active_cryptocurrencies,
        //     btcMarketCapitalPercentage : MARKET_DETAILS.data.market_cap_percentage.btc.toFixed(2),
        // }
        // console.log(newData)
        
        // return data
    }
    catch(error){
        // console.log(error);
    }


}

